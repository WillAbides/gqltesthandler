type Query {
  search(query: String!): [SearchResult!]!
}

union SearchResult = User | Repository

type User {
  id: ID!
  login: String!
}

type Repository {
  id: ID!
  name: String!
  owner: User!
}
