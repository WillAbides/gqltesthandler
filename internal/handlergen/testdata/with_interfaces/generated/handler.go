// Code generated by github.com/willabides/gqltesthandler/cmd/gqltesthandler. DO NOT EDIT.

package generated

import (
	"net/http"
	"sync"
)

// TestHandler is a mock GraphQL HTTP handler for testing.
// Set expectations with Expect methods, then use as an http.Handler.
type TestHandler struct {
	tb      TB
	handler http.Handler

	mu     sync.Mutex
	served bool

	feedExpectResponses     expectResponses[FeedVariables, feedResult]
	getNodeExpectResponses  expectResponses[GetNodeVariables, getNodeResult]
	entitiesExpectResponses expectResponses[EntitiesVariables, entitiesResult]
}

func (s *TestHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	s.handler.ServeHTTP(w, r)
}

// NewTestHandler creates a new TestHandler for use in tests.
func NewTestHandler(tb TB) *TestHandler {
	th := &TestHandler{tb: tb}
	th.handler = &testServer{tb: tb, handler: th}
	return th
}

// markServed records that the handler has processed a request. Once true,
// Reset and Reset<OpName> refuse to wipe state.
func (s *TestHandler) markServed() {
	s.mu.Lock()
	defer s.mu.Unlock()
	s.served = true
}

// Reset wipes all registered expectations and defaults on this handler and
// clears any pending cleanup errors so previously-unmet expectations will not
// fail the test. Reset records an error via tb.Errorf and leaves state
// untouched if the handler has already served any request (checked
// handler-wide, not per-operation), so it is safe to call only during test
// setup. The served-check and the clear are performed atomically — no
// request can mark the handler served while a Reset is in progress.
func (s *TestHandler) Reset() {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.Reset called after handler has served at least one request")
		return
	}
	s.feedExpectResponses.clear()
	s.getNodeExpectResponses.clear()
	s.entitiesExpectResponses.clear()
}

// feedResult is the result interface for the Feed operation.
type feedResult interface {
	writeFeedResult(w http.ResponseWriter, vars FeedVariables) error
}

type feedDataResult struct {
	data FeedResponse
}

func (r feedDataResult) writeFeedResult(w http.ResponseWriter, _ FeedVariables) error {
	return writeGraphQLData(w, r.data)
}

type feedErrorResult struct {
	errors []GraphQLError
}

func (r feedErrorResult) writeFeedResult(w http.ResponseWriter, _ FeedVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type feedRawResult struct {
	fn func(FeedVariables, http.ResponseWriter)
}

func (r feedRawResult) writeFeedResult(w http.ResponseWriter, vars FeedVariables) error {
	r.fn(vars, w)
	return nil
}

// FeedExpectation is a builder for setting the response of a Feed
// expectation or default.
type FeedExpectation struct {
	handler   *TestHandler
	vars      FeedVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *FeedExpectation) register(resp feedResult) {
	if b.isDefault {
		b.handler.feedExpectResponses.setDefault(resp)
		return
	}
	b.handler.feedExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *FeedExpectation) Respond(data FeedResponse) {
	b.register(feedDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *FeedExpectation) RespondError(errors ...GraphQLError) {
	b.register(feedErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *FeedExpectation) Handle(fn func(FeedVariables, http.ResponseWriter)) {
	b.register(feedRawResult{fn: fn})
}

// ExpectFeed sets up an expectation for the Feed operation.
func (s *TestHandler) ExpectFeed(vars FeedVariables, opts ...ExpectOption) *FeedExpectation {
	return &FeedExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultFeed returns a builder for the default Feed responder,
// used when no ExpectFeed matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultFeed() *FeedExpectation {
	return &FeedExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetFeed wipes registered expectations for the Feed operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetFeed records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetFeed(vars ...FeedVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetFeed called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.feedExpectResponses.clear()
		return
	}
	s.feedExpectResponses.clearByRequests(vars)
}

// getNodeResult is the result interface for the GetNode operation.
type getNodeResult interface {
	writeGetNodeResult(w http.ResponseWriter, vars GetNodeVariables) error
}

type getNodeDataResult struct {
	data GetNodeResponse
}

func (r getNodeDataResult) writeGetNodeResult(w http.ResponseWriter, _ GetNodeVariables) error {
	return writeGraphQLData(w, r.data)
}

type getNodeErrorResult struct {
	errors []GraphQLError
}

func (r getNodeErrorResult) writeGetNodeResult(w http.ResponseWriter, _ GetNodeVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type getNodeRawResult struct {
	fn func(GetNodeVariables, http.ResponseWriter)
}

func (r getNodeRawResult) writeGetNodeResult(w http.ResponseWriter, vars GetNodeVariables) error {
	r.fn(vars, w)
	return nil
}

// GetNodeExpectation is a builder for setting the response of a GetNode
// expectation or default.
type GetNodeExpectation struct {
	handler   *TestHandler
	vars      GetNodeVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *GetNodeExpectation) register(resp getNodeResult) {
	if b.isDefault {
		b.handler.getNodeExpectResponses.setDefault(resp)
		return
	}
	b.handler.getNodeExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *GetNodeExpectation) Respond(data GetNodeResponse) {
	b.register(getNodeDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *GetNodeExpectation) RespondError(errors ...GraphQLError) {
	b.register(getNodeErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *GetNodeExpectation) Handle(fn func(GetNodeVariables, http.ResponseWriter)) {
	b.register(getNodeRawResult{fn: fn})
}

// ExpectGetNode sets up an expectation for the GetNode operation.
func (s *TestHandler) ExpectGetNode(vars GetNodeVariables, opts ...ExpectOption) *GetNodeExpectation {
	return &GetNodeExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultGetNode returns a builder for the default GetNode responder,
// used when no ExpectGetNode matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultGetNode() *GetNodeExpectation {
	return &GetNodeExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetGetNode wipes registered expectations for the GetNode operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetGetNode records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetGetNode(vars ...GetNodeVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetGetNode called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.getNodeExpectResponses.clear()
		return
	}
	s.getNodeExpectResponses.clearByRequests(vars)
}

// entitiesResult is the result interface for the Entities operation.
type entitiesResult interface {
	writeEntitiesResult(w http.ResponseWriter, vars EntitiesVariables) error
}

type entitiesDataResult struct {
	data EntitiesResponse
}

func (r entitiesDataResult) writeEntitiesResult(w http.ResponseWriter, _ EntitiesVariables) error {
	return writeGraphQLData(w, r.data)
}

type entitiesErrorResult struct {
	errors []GraphQLError
}

func (r entitiesErrorResult) writeEntitiesResult(w http.ResponseWriter, _ EntitiesVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type entitiesRawResult struct {
	fn func(EntitiesVariables, http.ResponseWriter)
}

func (r entitiesRawResult) writeEntitiesResult(w http.ResponseWriter, vars EntitiesVariables) error {
	r.fn(vars, w)
	return nil
}

// EntitiesExpectation is a builder for setting the response of a Entities
// expectation or default.
type EntitiesExpectation struct {
	handler   *TestHandler
	vars      EntitiesVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *EntitiesExpectation) register(resp entitiesResult) {
	if b.isDefault {
		b.handler.entitiesExpectResponses.setDefault(resp)
		return
	}
	b.handler.entitiesExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *EntitiesExpectation) Respond(data EntitiesResponse) {
	b.register(entitiesDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *EntitiesExpectation) RespondError(errors ...GraphQLError) {
	b.register(entitiesErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *EntitiesExpectation) Handle(fn func(EntitiesVariables, http.ResponseWriter)) {
	b.register(entitiesRawResult{fn: fn})
}

// ExpectEntities sets up an expectation for the Entities operation.
func (s *TestHandler) ExpectEntities(vars EntitiesVariables, opts ...ExpectOption) *EntitiesExpectation {
	return &EntitiesExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultEntities returns a builder for the default Entities responder,
// used when no ExpectEntities matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultEntities() *EntitiesExpectation {
	return &EntitiesExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetEntities wipes registered expectations for the Entities operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetEntities records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetEntities(vars ...EntitiesVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetEntities called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.entitiesExpectResponses.clear()
		return
	}
	s.entitiesExpectResponses.clearByRequests(vars)
}
