// Code generated by github.com/willabides/gqltesthandler/cmd/gqltesthandler. DO NOT EDIT.

package generated

import (
	"net/http"
	"sync"
)

// TestHandler is a mock GraphQL HTTP handler for testing.
// Set expectations with Expect methods, then use as an http.Handler.
type TestHandler struct {
	tb      TB
	handler http.Handler

	mu     sync.Mutex
	served bool

	zeroFragmentExpectResponses     expectResponses[ZeroFragmentVariables, zeroFragmentResult]
	explicitTypenameExpectResponses expectResponses[ExplicitTypenameVariables, explicitTypenameResult]
	singleVariantExpectResponses    expectResponses[SingleVariantVariables, singleVariantResult]
	sharedSpreadExpectResponses     expectResponses[SharedSpreadVariables, sharedSpreadResult]
	sharedInlineExpectResponses     expectResponses[SharedInlineVariables, sharedInlineResult]
}

func (s *TestHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	s.handler.ServeHTTP(w, r)
}

// NewTestHandler creates a new TestHandler for use in tests.
func NewTestHandler(tb TB) *TestHandler {
	th := &TestHandler{tb: tb}
	th.handler = &testServer{tb: tb, handler: th}
	return th
}

// markServed records that the handler has processed a request. Once true,
// Reset and Reset<OpName> refuse to wipe state.
func (s *TestHandler) markServed() {
	s.mu.Lock()
	defer s.mu.Unlock()
	s.served = true
}

// Reset wipes all registered expectations and defaults on this handler and
// clears any pending cleanup errors so previously-unmet expectations will not
// fail the test. Reset records an error via tb.Errorf and leaves state
// untouched if the handler has already served any request (checked
// handler-wide, not per-operation), so it is safe to call only during test
// setup. The served-check and the clear are performed atomically — no
// request can mark the handler served while a Reset is in progress.
func (s *TestHandler) Reset() {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.Reset called after handler has served at least one request")
		return
	}
	s.zeroFragmentExpectResponses.clear()
	s.explicitTypenameExpectResponses.clear()
	s.singleVariantExpectResponses.clear()
	s.sharedSpreadExpectResponses.clear()
	s.sharedInlineExpectResponses.clear()
}

// zeroFragmentResult is the result interface for the ZeroFragment operation.
type zeroFragmentResult interface {
	writeZeroFragmentResult(w http.ResponseWriter, vars ZeroFragmentVariables) error
}

type zeroFragmentDataResult struct {
	data ZeroFragmentResponse
}

func (r zeroFragmentDataResult) writeZeroFragmentResult(w http.ResponseWriter, _ ZeroFragmentVariables) error {
	return writeGraphQLData(w, r.data)
}

type zeroFragmentErrorResult struct {
	errors []GraphQLError
}

func (r zeroFragmentErrorResult) writeZeroFragmentResult(w http.ResponseWriter, _ ZeroFragmentVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type zeroFragmentRawResult struct {
	fn func(ZeroFragmentVariables, http.ResponseWriter)
}

func (r zeroFragmentRawResult) writeZeroFragmentResult(w http.ResponseWriter, vars ZeroFragmentVariables) error {
	r.fn(vars, w)
	return nil
}

// ZeroFragmentExpectation is a builder for setting the response of a ZeroFragment
// expectation or default.
type ZeroFragmentExpectation struct {
	handler   *TestHandler
	vars      ZeroFragmentVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *ZeroFragmentExpectation) register(resp zeroFragmentResult) {
	if b.isDefault {
		b.handler.zeroFragmentExpectResponses.setDefault(resp)
		return
	}
	b.handler.zeroFragmentExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *ZeroFragmentExpectation) Respond(data ZeroFragmentResponse) {
	b.register(zeroFragmentDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *ZeroFragmentExpectation) RespondError(errors ...GraphQLError) {
	b.register(zeroFragmentErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *ZeroFragmentExpectation) Handle(fn func(ZeroFragmentVariables, http.ResponseWriter)) {
	b.register(zeroFragmentRawResult{fn: fn})
}

// ExpectZeroFragment sets up an expectation for the ZeroFragment operation.
func (s *TestHandler) ExpectZeroFragment(vars ZeroFragmentVariables, opts ...ExpectOption) *ZeroFragmentExpectation {
	return &ZeroFragmentExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultZeroFragment returns a builder for the default ZeroFragment responder,
// used when no ExpectZeroFragment matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultZeroFragment() *ZeroFragmentExpectation {
	return &ZeroFragmentExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetZeroFragment wipes registered expectations for the ZeroFragment operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetZeroFragment records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetZeroFragment(vars ...ZeroFragmentVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetZeroFragment called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.zeroFragmentExpectResponses.clear()
		return
	}
	s.zeroFragmentExpectResponses.clearByRequests(vars)
}

// explicitTypenameResult is the result interface for the ExplicitTypename operation.
type explicitTypenameResult interface {
	writeExplicitTypenameResult(w http.ResponseWriter, vars ExplicitTypenameVariables) error
}

type explicitTypenameDataResult struct {
	data ExplicitTypenameResponse
}

func (r explicitTypenameDataResult) writeExplicitTypenameResult(w http.ResponseWriter, _ ExplicitTypenameVariables) error {
	return writeGraphQLData(w, r.data)
}

type explicitTypenameErrorResult struct {
	errors []GraphQLError
}

func (r explicitTypenameErrorResult) writeExplicitTypenameResult(w http.ResponseWriter, _ ExplicitTypenameVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type explicitTypenameRawResult struct {
	fn func(ExplicitTypenameVariables, http.ResponseWriter)
}

func (r explicitTypenameRawResult) writeExplicitTypenameResult(w http.ResponseWriter, vars ExplicitTypenameVariables) error {
	r.fn(vars, w)
	return nil
}

// ExplicitTypenameExpectation is a builder for setting the response of a ExplicitTypename
// expectation or default.
type ExplicitTypenameExpectation struct {
	handler   *TestHandler
	vars      ExplicitTypenameVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *ExplicitTypenameExpectation) register(resp explicitTypenameResult) {
	if b.isDefault {
		b.handler.explicitTypenameExpectResponses.setDefault(resp)
		return
	}
	b.handler.explicitTypenameExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *ExplicitTypenameExpectation) Respond(data ExplicitTypenameResponse) {
	b.register(explicitTypenameDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *ExplicitTypenameExpectation) RespondError(errors ...GraphQLError) {
	b.register(explicitTypenameErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *ExplicitTypenameExpectation) Handle(fn func(ExplicitTypenameVariables, http.ResponseWriter)) {
	b.register(explicitTypenameRawResult{fn: fn})
}

// ExpectExplicitTypename sets up an expectation for the ExplicitTypename operation.
func (s *TestHandler) ExpectExplicitTypename(vars ExplicitTypenameVariables, opts ...ExpectOption) *ExplicitTypenameExpectation {
	return &ExplicitTypenameExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultExplicitTypename returns a builder for the default ExplicitTypename responder,
// used when no ExpectExplicitTypename matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultExplicitTypename() *ExplicitTypenameExpectation {
	return &ExplicitTypenameExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetExplicitTypename wipes registered expectations for the ExplicitTypename operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetExplicitTypename records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetExplicitTypename(vars ...ExplicitTypenameVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetExplicitTypename called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.explicitTypenameExpectResponses.clear()
		return
	}
	s.explicitTypenameExpectResponses.clearByRequests(vars)
}

// singleVariantResult is the result interface for the SingleVariant operation.
type singleVariantResult interface {
	writeSingleVariantResult(w http.ResponseWriter, vars SingleVariantVariables) error
}

type singleVariantDataResult struct {
	data SingleVariantResponse
}

func (r singleVariantDataResult) writeSingleVariantResult(w http.ResponseWriter, _ SingleVariantVariables) error {
	return writeGraphQLData(w, r.data)
}

type singleVariantErrorResult struct {
	errors []GraphQLError
}

func (r singleVariantErrorResult) writeSingleVariantResult(w http.ResponseWriter, _ SingleVariantVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type singleVariantRawResult struct {
	fn func(SingleVariantVariables, http.ResponseWriter)
}

func (r singleVariantRawResult) writeSingleVariantResult(w http.ResponseWriter, vars SingleVariantVariables) error {
	r.fn(vars, w)
	return nil
}

// SingleVariantExpectation is a builder for setting the response of a SingleVariant
// expectation or default.
type SingleVariantExpectation struct {
	handler   *TestHandler
	vars      SingleVariantVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *SingleVariantExpectation) register(resp singleVariantResult) {
	if b.isDefault {
		b.handler.singleVariantExpectResponses.setDefault(resp)
		return
	}
	b.handler.singleVariantExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *SingleVariantExpectation) Respond(data SingleVariantResponse) {
	b.register(singleVariantDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *SingleVariantExpectation) RespondError(errors ...GraphQLError) {
	b.register(singleVariantErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *SingleVariantExpectation) Handle(fn func(SingleVariantVariables, http.ResponseWriter)) {
	b.register(singleVariantRawResult{fn: fn})
}

// ExpectSingleVariant sets up an expectation for the SingleVariant operation.
func (s *TestHandler) ExpectSingleVariant(vars SingleVariantVariables, opts ...ExpectOption) *SingleVariantExpectation {
	return &SingleVariantExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultSingleVariant returns a builder for the default SingleVariant responder,
// used when no ExpectSingleVariant matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultSingleVariant() *SingleVariantExpectation {
	return &SingleVariantExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetSingleVariant wipes registered expectations for the SingleVariant operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetSingleVariant records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetSingleVariant(vars ...SingleVariantVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetSingleVariant called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.singleVariantExpectResponses.clear()
		return
	}
	s.singleVariantExpectResponses.clearByRequests(vars)
}

// sharedSpreadResult is the result interface for the SharedSpread operation.
type sharedSpreadResult interface {
	writeSharedSpreadResult(w http.ResponseWriter, vars SharedSpreadVariables) error
}

type sharedSpreadDataResult struct {
	data SharedSpreadResponse
}

func (r sharedSpreadDataResult) writeSharedSpreadResult(w http.ResponseWriter, _ SharedSpreadVariables) error {
	return writeGraphQLData(w, r.data)
}

type sharedSpreadErrorResult struct {
	errors []GraphQLError
}

func (r sharedSpreadErrorResult) writeSharedSpreadResult(w http.ResponseWriter, _ SharedSpreadVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type sharedSpreadRawResult struct {
	fn func(SharedSpreadVariables, http.ResponseWriter)
}

func (r sharedSpreadRawResult) writeSharedSpreadResult(w http.ResponseWriter, vars SharedSpreadVariables) error {
	r.fn(vars, w)
	return nil
}

// SharedSpreadExpectation is a builder for setting the response of a SharedSpread
// expectation or default.
type SharedSpreadExpectation struct {
	handler   *TestHandler
	vars      SharedSpreadVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *SharedSpreadExpectation) register(resp sharedSpreadResult) {
	if b.isDefault {
		b.handler.sharedSpreadExpectResponses.setDefault(resp)
		return
	}
	b.handler.sharedSpreadExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *SharedSpreadExpectation) Respond(data SharedSpreadResponse) {
	b.register(sharedSpreadDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *SharedSpreadExpectation) RespondError(errors ...GraphQLError) {
	b.register(sharedSpreadErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *SharedSpreadExpectation) Handle(fn func(SharedSpreadVariables, http.ResponseWriter)) {
	b.register(sharedSpreadRawResult{fn: fn})
}

// ExpectSharedSpread sets up an expectation for the SharedSpread operation.
func (s *TestHandler) ExpectSharedSpread(vars SharedSpreadVariables, opts ...ExpectOption) *SharedSpreadExpectation {
	return &SharedSpreadExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultSharedSpread returns a builder for the default SharedSpread responder,
// used when no ExpectSharedSpread matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultSharedSpread() *SharedSpreadExpectation {
	return &SharedSpreadExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetSharedSpread wipes registered expectations for the SharedSpread operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetSharedSpread records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetSharedSpread(vars ...SharedSpreadVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetSharedSpread called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.sharedSpreadExpectResponses.clear()
		return
	}
	s.sharedSpreadExpectResponses.clearByRequests(vars)
}

// sharedInlineResult is the result interface for the SharedInline operation.
type sharedInlineResult interface {
	writeSharedInlineResult(w http.ResponseWriter, vars SharedInlineVariables) error
}

type sharedInlineDataResult struct {
	data SharedInlineResponse
}

func (r sharedInlineDataResult) writeSharedInlineResult(w http.ResponseWriter, _ SharedInlineVariables) error {
	return writeGraphQLData(w, r.data)
}

type sharedInlineErrorResult struct {
	errors []GraphQLError
}

func (r sharedInlineErrorResult) writeSharedInlineResult(w http.ResponseWriter, _ SharedInlineVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type sharedInlineRawResult struct {
	fn func(SharedInlineVariables, http.ResponseWriter)
}

func (r sharedInlineRawResult) writeSharedInlineResult(w http.ResponseWriter, vars SharedInlineVariables) error {
	r.fn(vars, w)
	return nil
}

// SharedInlineExpectation is a builder for setting the response of a SharedInline
// expectation or default.
type SharedInlineExpectation struct {
	handler   *TestHandler
	vars      SharedInlineVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *SharedInlineExpectation) register(resp sharedInlineResult) {
	if b.isDefault {
		b.handler.sharedInlineExpectResponses.setDefault(resp)
		return
	}
	b.handler.sharedInlineExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *SharedInlineExpectation) Respond(data SharedInlineResponse) {
	b.register(sharedInlineDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *SharedInlineExpectation) RespondError(errors ...GraphQLError) {
	b.register(sharedInlineErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *SharedInlineExpectation) Handle(fn func(SharedInlineVariables, http.ResponseWriter)) {
	b.register(sharedInlineRawResult{fn: fn})
}

// ExpectSharedInline sets up an expectation for the SharedInline operation.
func (s *TestHandler) ExpectSharedInline(vars SharedInlineVariables, opts ...ExpectOption) *SharedInlineExpectation {
	return &SharedInlineExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultSharedInline returns a builder for the default SharedInline responder,
// used when no ExpectSharedInline matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultSharedInline() *SharedInlineExpectation {
	return &SharedInlineExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetSharedInline wipes registered expectations for the SharedInline operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetSharedInline records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetSharedInline(vars ...SharedInlineVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetSharedInline called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.sharedInlineExpectResponses.clear()
		return
	}
	s.sharedInlineExpectResponses.clearByRequests(vars)
}
