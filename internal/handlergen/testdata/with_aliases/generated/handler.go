// Code generated by github.com/willabides/gqltesthandler/cmd/gqltesthandler. DO NOT EDIT.

package generated

import (
	"net/http"
	"sync"
)

// TestHandler is a mock GraphQL HTTP handler for testing.
// Set expectations with Expect methods, then use as an http.Handler.
type TestHandler struct {
	tb      TB
	handler http.Handler

	mu     sync.Mutex
	served bool

	aliasesExpectResponses   expectResponses[AliasesVariables, aliasesResult]
	aliasSwapExpectResponses expectResponses[AliasSwapVariables, aliasSwapResult]
}

func (s *TestHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	s.handler.ServeHTTP(w, r)
}

// NewTestHandler creates a new TestHandler for use in tests.
func NewTestHandler(tb TB) *TestHandler {
	th := &TestHandler{tb: tb}
	th.handler = &testServer{tb: tb, handler: th}
	return th
}

// markServed records that the handler has processed a request. Once true,
// Reset and Reset<OpName> refuse to wipe state.
func (s *TestHandler) markServed() {
	s.mu.Lock()
	defer s.mu.Unlock()
	s.served = true
}

// Reset wipes all registered expectations and defaults on this handler and
// clears any pending cleanup errors so previously-unmet expectations will not
// fail the test. Reset records an error via tb.Errorf and leaves state
// untouched if the handler has already served any request (checked
// handler-wide, not per-operation), so it is safe to call only during test
// setup. The served-check and the clear are performed atomically — no
// request can mark the handler served while a Reset is in progress.
func (s *TestHandler) Reset() {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.Reset called after handler has served at least one request")
		return
	}
	s.aliasesExpectResponses.clear()
	s.aliasSwapExpectResponses.clear()
}

// aliasesResult is the result interface for the Aliases operation.
type aliasesResult interface {
	writeAliasesResult(w http.ResponseWriter, vars AliasesVariables) error
}

type aliasesDataResult struct {
	data AliasesResponse
}

func (r aliasesDataResult) writeAliasesResult(w http.ResponseWriter, _ AliasesVariables) error {
	return writeGraphQLData(w, r.data)
}

type aliasesErrorResult struct {
	errors []GraphQLError
}

func (r aliasesErrorResult) writeAliasesResult(w http.ResponseWriter, _ AliasesVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type aliasesRawResult struct {
	fn func(AliasesVariables, http.ResponseWriter)
}

func (r aliasesRawResult) writeAliasesResult(w http.ResponseWriter, vars AliasesVariables) error {
	r.fn(vars, w)
	return nil
}

// AliasesExpectation is a builder for setting the response of a Aliases
// expectation or default.
type AliasesExpectation struct {
	handler   *TestHandler
	vars      AliasesVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *AliasesExpectation) register(resp aliasesResult) {
	if b.isDefault {
		b.handler.aliasesExpectResponses.setDefault(resp)
		return
	}
	b.handler.aliasesExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *AliasesExpectation) Respond(data AliasesResponse) {
	b.register(aliasesDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *AliasesExpectation) RespondError(errors ...GraphQLError) {
	b.register(aliasesErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *AliasesExpectation) Handle(fn func(AliasesVariables, http.ResponseWriter)) {
	b.register(aliasesRawResult{fn: fn})
}

// ExpectAliases sets up an expectation for the Aliases operation.
func (s *TestHandler) ExpectAliases(vars AliasesVariables, opts ...ExpectOption) *AliasesExpectation {
	return &AliasesExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultAliases returns a builder for the default Aliases responder,
// used when no ExpectAliases matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultAliases() *AliasesExpectation {
	return &AliasesExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetAliases wipes registered expectations for the Aliases operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetAliases records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetAliases(vars ...AliasesVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetAliases called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.aliasesExpectResponses.clear()
		return
	}
	s.aliasesExpectResponses.clearByRequests(vars)
}

// aliasSwapResult is the result interface for the AliasSwap operation.
type aliasSwapResult interface {
	writeAliasSwapResult(w http.ResponseWriter, vars AliasSwapVariables) error
}

type aliasSwapDataResult struct {
	data AliasSwapResponse
}

func (r aliasSwapDataResult) writeAliasSwapResult(w http.ResponseWriter, _ AliasSwapVariables) error {
	return writeGraphQLData(w, r.data)
}

type aliasSwapErrorResult struct {
	errors []GraphQLError
}

func (r aliasSwapErrorResult) writeAliasSwapResult(w http.ResponseWriter, _ AliasSwapVariables) error {
	return writeGraphQLErrors(w, r.errors)
}

type aliasSwapRawResult struct {
	fn func(AliasSwapVariables, http.ResponseWriter)
}

func (r aliasSwapRawResult) writeAliasSwapResult(w http.ResponseWriter, vars AliasSwapVariables) error {
	r.fn(vars, w)
	return nil
}

// AliasSwapExpectation is a builder for setting the response of a AliasSwap
// expectation or default.
type AliasSwapExpectation struct {
	handler   *TestHandler
	vars      AliasSwapVariables
	opts      []ExpectOption
	isDefault bool
}

func (b *AliasSwapExpectation) register(resp aliasSwapResult) {
	if b.isDefault {
		b.handler.aliasSwapExpectResponses.setDefault(resp)
		return
	}
	b.handler.aliasSwapExpectResponses.expect(b.handler.tb, b.vars, nil, resp, b.opts...)
}

// Respond sets the expectation to return the given data.
func (b *AliasSwapExpectation) Respond(data AliasSwapResponse) {
	b.register(aliasSwapDataResult{data: data})
}

// RespondError sets the expectation to return GraphQL errors.
func (b *AliasSwapExpectation) RespondError(errors ...GraphQLError) {
	b.register(aliasSwapErrorResult{errors: errors})
}

// Handle sets the response to invoke a custom handler function. The function
// receives the variables from the incoming request.
func (b *AliasSwapExpectation) Handle(fn func(AliasSwapVariables, http.ResponseWriter)) {
	b.register(aliasSwapRawResult{fn: fn})
}

// ExpectAliasSwap sets up an expectation for the AliasSwap operation.
func (s *TestHandler) ExpectAliasSwap(vars AliasSwapVariables, opts ...ExpectOption) *AliasSwapExpectation {
	return &AliasSwapExpectation{
		handler: s,
		vars:    vars,
		opts:    opts,
	}
}

// DefaultAliasSwap returns a builder for the default AliasSwap responder,
// used when no ExpectAliasSwap matches an incoming request. Defaults are
// infinitely callable, never fail at cleanup, and a subsequent call replaces
// the previous default.
func (s *TestHandler) DefaultAliasSwap() *AliasSwapExpectation {
	return &AliasSwapExpectation{
		handler:   s,
		isDefault: true,
	}
}

// ResetAliasSwap wipes registered expectations for the AliasSwap operation
// and disarms their cleanup errors. With no arguments it also clears the
// default responder. With one or more vars it wipes only expectations whose
// key matches; the default and non-matching expectations are left untouched.
// A vars entry that matches nothing is a no-op for that entry. If the handler
// has already served any request (checked handler-wide, not per-operation),
// ResetAliasSwap records an error via tb.Errorf and leaves state untouched.
func (s *TestHandler) ResetAliasSwap(vars ...AliasSwapVariables) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.served {
		s.tb.Errorf("TestHandler.ResetAliasSwap called after handler has served at least one request")
		return
	}
	if len(vars) == 0 {
		s.aliasSwapExpectResponses.clear()
		return
	}
	s.aliasSwapExpectResponses.clearByRequests(vars)
}
